import { infrastructureCommon } from './infrastructure-common';

describe('infrastructureCommon', () => {
  it('should work', () => {
    expect(infrastructureCommon()).toEqual('infrastructure-common');
  });
});
